version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: piknik_postgres
    restart: always
    environment:
      POSTGRES_DB: piknik_ecopark
      POSTGRES_USER: piknik_user
      POSTGRES_PASSWORD: '12345'
    ports:
      - "5433:5432"